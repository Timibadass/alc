extends layout

block content
  .heading
    h2= title 
  ul.resources
    each course in courses
      li.resources-grid
        a(href=`/resources/${course._id}`)= course._id
        span ( #{course.count} Resources)
  