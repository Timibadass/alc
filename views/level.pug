extends layout

include mixins/studentCard

block content
  h2 #{levels} Level
  ul
    each l in level
      li
        a(href=`/levels/${l._id}`)= l._id
        span (#{l.count})
  .students  
    each student in students
      .card
        +studentCard(student)